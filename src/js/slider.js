document.addEventListener("DOMContentLoaded", function () {
    slider = new SimpleAdaptiveSlider("#slider-1", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-2", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-3", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-4", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-5", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-6", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-10", {
        autoplay: false,
        interval: 10000,
    });

    slider = new SimpleAdaptiveSlider("#slider-11", {
        autoplay: false,
        interval: 10000,
    });
});
